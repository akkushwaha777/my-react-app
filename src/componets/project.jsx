import React from 'react';

function Project() {
  return (
    <div className="container my-3">
      <h3>Project</h3>
      <p className="text-muted mb-2">Current focus: polish the UI and keep tasks synced via local storage.</p>
      <p className="text-muted">Next steps: hook up a backend API and add authentication when ready.</p>
    </div>
  );
}

export default Project;
